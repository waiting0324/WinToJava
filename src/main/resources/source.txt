if len(trim(dw_criteria.virtual_account)) > 0 then

if len(trim(ls_register_no)) <= 0 and ls_custom_flag = 'Y' then

if len(trim(ls_close_flag)) > 0 and ls_close_flag <> 'A' then

if isnull(ls_start_date) or isnull(ls_end_date) or isnull(ls_payment_type) or isnull(ls_cust_attr)  or isnull(ls_ar_item) then

if ll_ar_amt - ll_ar_rec_amt >= ll_ar_milus_amt then