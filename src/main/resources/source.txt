choose case ls_auto
       case 'V'  // 使用虛擬帳號銷帳
               is_where += " and nvl(auto,'N') ='Y' and ar_trade.virtual_account is not null and upd_user = 'system'"
       case 'Y'  // 使用下載EXCEL檔案 沖銷
               is_where += " and nvl(auto,'N') ='Y' and ar_trade.virtual_account is null"
       case 'N'
               is_where += " and nvl(auto,'N') ='" + ls_auto + "'"
end choose
