if len(trim(ls_end_date)) = 16 then
                 ls_start_date = substr(ls_end_date,1,8)
                 ls_end_date   = substr(ls_end_date,9)
                 
                 json = call API AR_RECEIVE_PAYMENT(str gs_uinfo.u_cargo_loca, str data, str ls_start_date, str ls_end_date, int ll_ar_milus_amt)
                 自 json 中取 ls_payment_type, ls_cust_attr, ls_ar_item
             end if
